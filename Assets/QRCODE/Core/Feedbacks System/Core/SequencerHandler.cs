using QRCode.Events;
using Sirenix.OdinInspector;
using UnityEngine;

namespace QRCode.Feedbacks
{
    public class SequencerHandler : FeedbackGenericHandler<Sequencer>
    {
        /*[SerializeField] private bool playOnStart = false;
        public Sequencer Sequencer = new Sequencer();

        private void Start()
        {
            Sequencer.SetOwnerForAll(this);
            if (playOnStart) Sequencer.Play();
        }

        public void Play()
        {
            Sequencer.Play();
        }*/
    }
}
